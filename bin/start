#!/usr/bin/env ruby
require 'rubygems'
require 'bundler/setup'

require 'retirement_swap'

processor = RetirementSwap.start(ENV.fetch("RAILS_ENV"))

puts "RetirementSwap Started."
loop do
  processor.run
end
