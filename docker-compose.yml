postgres:
  image: postgres
  environment:
    - "POSTGRES_USER=nero"
    - "POSTGRES_PASSWORD=nero"
  ports:
    - "5432:5432"

nero:
  dockerfile: Dockerfile
  build: ./
  env_file:
    - .env
  volumes:
    - ./:/app
  links:
    - postgres
